# FluNET 1.0 release gate

**Status: GATE IMPLEMENTED — RELEASE BLOCKED / VERIFICATION ENVIRONMENT BLOCKED.**

Batch 89 deliberately does not equate source completion with release completion.

## Current evidence state

The release surface is now wired correctly:

- `FluNET.Tool` is part of `FluNET.sln` and targets `net9.0`;
- `.github/workflows/ci.yml` installs .NET 9 and invokes the canonical release scripts on Linux/macOS/Windows;
- Bash uses `scripts/verify-release.sh`;
- Windows uses `scripts/verify-release.ps1`, with native-command failures promoted to PowerShell errors.

However, the gate still has **no build/test evidence**.

Observed GitHub runs:

- run `31842282135` for `794d1cb2...` completed as failure before any job steps;
- after the CI .NET 9 correction, run `31895032893` for `83add669...` also completed as failure before any job steps;
- GitHub's check annotation states: `The job was not started because your account is locked due to a billing issue.`

The local execution environment also has no `dotnet` executable and cannot resolve external hosts needed to bootstrap the SDK. No GitHub workflow was manually triggered or rerun.

Therefore there is currently **no valid evidence** for restore, Release build, Release tests, Tool smoke checks, Tool packing, or local Tool installation. Public `StandardLanguageIdentity.Version` remains `0.3`; `FluNET.Tool` package metadata remains `0.3.0-preview`.

## Gate policy

`ReleasePromotionGate` models the evidence requirement without executing external commands. Required checks are:

1. restore;
2. Release solution build;
3. Release tests;
4. static contract checks;
5. direct Tool smoke;
6. Tool pack;
7. local Tool install smoke.

With complete green evidence while the public language identity is still `0.3`, the gate permits only an isolated **version-promotion commit**. It does not permit a release yet. The complete gate must then be rerun on the promoted exact tree; only complete evidence with the public identity aligned to `1.0` permits release/tagging.

## Canonical executable gate

```bash
./scripts/verify-release.sh
```

or:

```powershell
./scripts/verify-release.ps1
```

GitHub CI invokes these same scripts. A runner/infrastructure failure before checkout is not compiler/test evidence and must not be interpreted as a failed or successful FluNET build.

## Current decision

**Do not bump `StandardLanguageIdentity.Version`, do not change the Tool package to `1.0.0`, and do not create a 1.0 tag/release yet.**

The implementation scope is source-complete; the remaining blocker is an executable .NET 9 verification environment plus any stabilization fixes revealed by the real gate.
