name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
<<<<<<< HEAD
  release-gate:
=======
  build-and-test:
>>>>>>> origin/agent/stabilize-poc-foundation
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
<<<<<<< HEAD
          dotnet-version: 9.0.x

      - name: Verify release gate (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: bash ./scripts/verify-release.sh

      - name: Verify release gate (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: ./scripts/verify-release.ps1
=======
          dotnet-version: 8.0.x
      - name: Restore
        run: dotnet restore FluNET.sln
      - name: Build
        run: dotnet build FluNET.sln --configuration Release --no-restore
      - name: Test
        run: dotnet test FluNET.sln --configuration Release --no-build --logger "console;verbosity=normal"
>>>>>>> origin/agent/stabilize-poc-foundation
